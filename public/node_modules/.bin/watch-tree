#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../watch-tree/bin/watch-tree" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../watch-tree/bin/watch-tree" "$@"
  ret=$?
fi
exit $ret
